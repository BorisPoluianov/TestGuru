<h2><%= t('.all_badges') %></h2>

<table class="table">
  <thead class="thead-light">
    <tr>
      <th class="text-center"><%= t('.image') %></th>
      <th><%= t('.title') %></th>
      <th><%= t('.rule_parameter') %></th>
      <th colspan="2"><%= t('.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @badges %>
  </tbody>
</table>

<div>
  <%= link_to t('.new_badge'), new_admin_badge_path, class: 'btn btn-primary' %>
</div>

<br>

<div>
  <%= link_to t('back'), admin_tests_path, class: 'link'%>
</div>
