<tr>
  <td class="text-center"><%= image_tag((badge.image), class: 'rounded-circle', height: '50px') %></td>
  <td><%= badge.title %></td>
  <td><%= badge.rule_parameter %></td>
  <td><%= link_to t('edit'), edit_admin_badge_path(badge) %></td>
  <td><%= link_to t('delete'), admin_badge_path(badge),
    method: :delete, data: { confirm: t('sure?') } %></td>
</tr>
