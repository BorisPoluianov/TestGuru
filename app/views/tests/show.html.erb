<h1><%= @test.title %></h1>
<h2>Category: <%= @test.category.title %>, Level: <%= @test.level %></h2>

<div>
  <table>
    <thead>
      <tr>
        <th>question</th>
        <th colspan="3">actions</th>
      </tr>
    </thead>
    <tbody>
      <%= render @test.questions %>
    </tbody>
  </table>

  <%= link_to 'New question', new_test_question_path(@test) %>
</div>

<br/>
<%= link_to 'Back to tests', root_path %>
