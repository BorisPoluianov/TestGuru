<%= form_with(model: test_progress, local: true) do |form| %>
  <% if test_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_progress.errors.count, "error") %> prohibited this test_progress from being saved:</h2>

      <ul>
        <% test_progress.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
